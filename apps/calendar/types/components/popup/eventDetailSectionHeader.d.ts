import { h } from 'preact';
import type EventModel from "../../model/eventModel";
interface Props {
    event: EventModel;
}
export declare function EventDetailSectionHeader({ event }: Props): h.JSX.Element;
export {};
