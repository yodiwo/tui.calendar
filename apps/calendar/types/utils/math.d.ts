export declare function limit(value: number, minArr: number[], maxArr: number[]): number;
/**
 * a : b = y : x;
 * ==
 * x = (b * y) / a;
 */
export declare function ratio(a: number, b: number, y: number): number;
export declare function isBetween(value: number, min: number, max: number): boolean;
